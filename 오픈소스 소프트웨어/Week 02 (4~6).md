# 설치와 실행

## 1. 홈페이지와 다운로드

```
✓ 홈페이지 
 - https://git-scm.com/
✓ 오픈소스 소프트웨어 OSS 주소
 - https://github.com/git/git
```
```
✓ 편집기 설정
- Visual Studio code로 설정
✓ 기본 브랜치 이름 설정
- $ git config --global init.defaultBranch main
✓ 깃 실행
- git bash 이용
```
```
✓ CLI 인터페이스
- Command Line Interface
  - 명령 프롬프트와 같은 인터페이스 
✓ 리눅스 명령어
  - $ pwdㅎ
  - $ Is-al
✓ git 명령어 기초
 - 버전 확인 : $ git --version
 - 설정 확인 : $ git config --list
 - 도움 : $ git --help
✓ 깃 설정 명령 구조 
 - $ git config --설정범위 설정변수 설정값
 - 설정 범위 :[--system|--global|--local]
    ㄴ system : 모든 사용자 / global :현재 사용자의 모든 저장소 / local : 현재 사용자의 현재 저장소
```
## 1-1 깃 설정
```
✓ --gloabal 옵선
  ㄴ 내용은 .gitconfig 파일에 저장 
✓ 전역 설정 파일 편집
 - 옵션 --edit이나 -e를 사용
✓ 설정변수 core.autocrlf로 true 설정 
 - 윈도에서 autocrlf를 true로 설정
 - 맥과 리눅스에서 input으로 설정
✓ 줄바꿈 설정 이유
 - 윈도우 : CR
 - mac / linux :LF
```
## 1-2 깃 저장소 생성
```
✓ 2가지 방식
 - $ git init
  ㄴ 현재 디렉토리를 git repository로 만들기 위해서 사용
 - git init basic
   ㄴ 현재 폴더 하부에서 폴더 basic을 생성하고 git repository로 만들기 위해서 사용
```
## 2. 리눅스 명령
```
✓ 폴더 관련 명령
 - $ pwd(Print Working Directory) = 현재 폴더 표시
 - $ cd(Change Directory)
 - $ mkdir dname(Make Directory)
 - $ ls(File or folder list)
 - 다양한 옵션
  ㄴ -l : 파일의 상세 정보
  ㄴ -a : 숨김 파일 표시
  ㄴ -al : 위 두개를 조합
```
```
✓ 파일 관련 명령
 - 생성 :
  - $ touch fname : 빈 파일 fname 생성
  - 표준 출력으로 결과 표시 : echo
   - $ echo git bash
   - $ echo 'print()'
     ㄴ 이후 문자열을 컴퓨터 터미널에 출력하는 명령어
  - $ cat fname : 파일 내용 보이기
  - $ cp a b : 파일 a를 b로 복사
  - $ mv f1 f2 : 파일 f1을 f2로 이름 수정
  - $ rm fname : 파일 fname 삭제
  - $ rm -rf dname : 폴더 삭제, 옵션 사용
     ㄴ -f : 강제로 파일이나 디렉토리를 삭제
     ㄴ -r : 디렉토리 내부의 모든 내용을 삭제
```
```
✓ 명령어 ls
 - ls -l : 파일의 상세 정보
 - ls -a : 숨김 파일 표시
 - ls -t : 파일들을 생성 시간 순(제일 최신 것)으로 표시
 - ls -rt : 파일들을 생성 시간 순(제일 오래된 것)으로 표시
 - ls -f : 파일 표시 시 마지막 유형에 나타내는 파일명을 끝에 표시
   ㄴ '/' : 디렉터리, '*' : 실행파일, '@' : 링크 등등
```
```
✓ 명령어 cat(Concatenate)
 - 파일의 내용을 화면에 출력
 - cat file 1
   ㄴ file 1의 내용을 출력
 - cat file 1 file 2 | more : file 1과 file 2의 내용을 페이지별로 출력
 - cat file 1 file 2 | head : 처음부터 10번째 줄까지만 출력
 - cat file 1 file 2 | tail : 끝에서 10번째 줄까지만 출력
✓ Redirection 명령어 연산
 - '>' 기호 : 기존에 있는 파일 내용을 지우고 저장
 - '>>' 기호 : 기존 파일 내용 뒤에 덧붙여서 저장
 - '<' 기호 : 파일의 데이터르 명령에 입력
✓ 삭제 후 다시 저장소 설정
 - $ rm -rf.git
 ```
   